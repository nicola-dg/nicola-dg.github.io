
// @ts-nocheck
const Title = ({ children }) => {
    return (
        <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold">
            {children}
        </h1>
    );
};

export default Title;
